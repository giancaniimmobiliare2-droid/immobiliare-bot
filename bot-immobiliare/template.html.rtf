<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Proposte Esclusive</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background: #f4f6f9; font-family: 'Helvetica Neue', sans-serif; }
        .header { background: #004d40; color: white; padding: 40px 0; margin-bottom: 30px; }
        .card { border: none; box-shadow: 0 4px 15px rgba(0,0,0,0.1); border-radius: 12px; transition: transform 0.3s; }
        .card:hover { transform: translateY(-5px); }
        .btn-fb { background-color: #1877F2; color: white; border: none; width: 100%; }
        .btn-ig { background: linear-gradient(45deg, #405de6, #5851db, #833ab4, #c13584, #e1306c, #fd1d1d); color: white; border: none; width: 100%; }
    </style>
</head>
<body>
    <div class="header text-center">
        <h1>Ciao {{ nome_cliente }}!</h1>
        <p>Ho selezionato questi immobili per te questo mese.</p>
    </div>
    <div class="container pb-5">
        <div class="row">
            {% for casa in immobili %}
            <div class="col-md-6 col-lg-4 mb-4">
                <div class="card h-100 p-3">
                    <div class="card-body">
                        <h4 class="card-title">{{ casa.Titolo }}</h4>
                        <h5 class="text-primary fw-bold">{{ casa.Prezzo }}</h5>
                        <p class="card-text text-muted">{{ casa.Descrizione }}</p>
                        
                        {% if 'facebook' in casa.Link %}
                            <a href="{{ casa.Link }}" target="_blank" class="btn btn-fb py-2">📺 Guarda su Facebook</a>
                        {% elif 'instagram' in casa.Link %}
                            <a href="{{ casa.Link }}" target="_blank" class="btn btn-ig py-2">📸 Guarda su Instagram</a>
                        {% else %}
                            <a href="{{ casa.Link }}" target="_blank" class="btn btn-dark py-2">🔗 Vai all'annuncio</a>
                        {% endif %}
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
</body>
</html>